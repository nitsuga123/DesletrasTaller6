using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using System.Collections.Generic;

public class InputManager : MonoBehaviour {

	/*
	public string[] respuestas;

	[SerializeField]
	private InputField inputfield;
	[SerializeField]
	private Text Warning;

	GameObject cont = GameObject.Find ("cont");
	GameMaster inputcont;
	public int Contador;
	private int contadorJugador;


	// Use this for initialization
	void Awake(){
		
		Contador= inputcont.GetInputCont();
		contadorJugador = 0;


	}
	void Start () {
		respuestas = new string[3];

	}
	
	// Update is called once per frame
	void Update () {
	


	}

	void Respuestas(){
		if (inputfield.text == "") {
			Warning.text="No se ha escrito nada";
			Contador--;
			inputcont.SetInputCont (Contador);
		} else {
			if (contadorJugador == 0) {
				respuestas [0] = inputfield.text;
				contadorJugador++;
			} else if (contadorJugador == 1) {
				respuestas [1] = inputfield.text;
				contadorJugador++;
			} else {
				respuestas [2] = inputfield.text;
				contadorJugador=0;
			} 
		}

	}

*/
}
